---
import BlogPost from '../../../layouts/BlogPost.astro';
import { postPaths } from '../../../components/postPaths';
export async function getStaticPaths() { return postPaths('zh'); }
const { entry, hasAlternate } = Astro.props;
---
<BlogPost entry={entry} hasAlternate={hasAlternate} />
